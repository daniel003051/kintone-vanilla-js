{
  "name": "kintone-vanilla-js",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "kintone-uploader-env && rsbuild dev",
    "build": "rsbuild build && kintone-uploader-env -m ./mainfests/prod.json",
    "preview": "rsbuild preview"
  },
  "devDependencies": {
    "@kintone/customize-uploader": "^8.0.13",
    "@rsbuild/core": "1.0.1-beta.3",
    "@rsbuild/plugin-basic-ssl": "^1.1.0",
    "kintone-uploader-env": "^1.1.0"
  }
}